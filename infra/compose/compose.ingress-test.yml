services:
  arena-api:
    image: alpine:3.23
    command: [sh, -c, 'while true; do printf "ok\n" | nc -l -p 3001; done']
    healthcheck: { disable: true }
  arena-web:
    image: alpine:3.23
    command: [sh, -c, 'while true; do printf "ok\n" | nc -l -p 3000; done']
